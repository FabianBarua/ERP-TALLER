# Local: docker compose -f docker-compose.yml -f docker-compose.local.yml up -d --build
services:
  db:
    ports:
      - "${DB_PORT:-5432}:5432"

  front:
    ports:
      - "${APP_PORT:-3000}:80"

networks:
  dokploy-network:
    external: false
    driver: bridge
